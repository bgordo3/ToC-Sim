/*global $, Scenarios */
var app = app || {};


//Defines our Station objects. 
var ResourceItem = function () {
    var name = '',
        provider = [0],
        required = 1,
        priority = 0;
};